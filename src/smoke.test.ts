import { describe, expect, it } from 'vitest';

/**
 * Smoke test — proves the Vitest harness is wired up in the skeleton slice.
 * Replaced by real domain tests as feature slices land.
 */
describe('test harness', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
